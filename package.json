{
  "name": "react-native-scrollview-indicator",
  "version": "1.0.1",
  "description": "A ScrollView with custom scrollindicator for vertical scrolling. When active the indicator is always visible",
  "private": false,
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "author": {
    "name": "Ruben"
  },
  "peerDependencies": {
    "react": ">= 15.x",
    "react-native": ">= 0.47.x"
  }
}
